using UnityEngine;

public class printer : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("LALILA I'm a hot-dog!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
